import 'package:flutter/material.dart';
import 'package:get/get.dart';

class ColorChipList extends StatelessWidget {
  const ColorChipList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return Container(
      child: Row(
        children: []
      ),
    );
  }
}
