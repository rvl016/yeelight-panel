import 'package:flutter/cupertino.dart';

class AnimatedDeviceControPanelLayout extends StatelessWidget {
  const AnimatedDeviceControPanelLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
