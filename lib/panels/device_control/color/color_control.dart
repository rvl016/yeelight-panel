import 'package:flutter/material.dart';

class ColorControl extends StatelessWidget {
  const ColorControl({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
