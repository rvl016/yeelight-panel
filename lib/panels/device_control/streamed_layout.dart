import 'package:flutter/cupertino.dart';

class StreamedDeviceControPanelLayout extends StatelessWidget {
  const StreamedDeviceControPanelLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
